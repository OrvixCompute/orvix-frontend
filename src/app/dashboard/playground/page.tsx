import { Playground } from "@/components/dashboard/playground/Playground";

export default function PlaygroundPage() {
  return <Playground subtitle="Test inference against the network, right from the dashboard." />;
}
